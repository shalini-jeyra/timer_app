part of styles;
class BoxBorders{
  static const primaryBoxBorder=BorderRadius.all(
              Radius.circular(6));
}

class ButtonBorders{
   static const primaryBoxBorder=BorderRadius.all(
              Radius.circular(16));
}